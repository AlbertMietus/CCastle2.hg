.. _castle-RPy-buildIn:

BuildIn (into RPy)
==================

.. note:: Only on my local laptop, for now

.. toctree::
   :glob:

   *
   */index

Every writer, so also RPy, depend on some functions and data-structures that are `build in` -- that is: library code to
support the generated code. That code is documented here.
